{
  "name": "frida-system-server-agent",
  "version": "1.0.0",
  "description": "Agent used by Frida's Linux backend on Android",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile index.js -o system-server.js -S -c"
  },
  "devDependencies": {
    "@types/frida-gum": "^18.8.2",
    "frida-compile": "^16.4.2"
  },
  "dependencies": {
    "frida-java-bridge": "^7.0.0"
  }
}
