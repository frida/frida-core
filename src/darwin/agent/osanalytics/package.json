{
  "name": "frida-osanalytics-agent",
  "version": "1.0.0",
  "description": "Agent used by Frida's Darwin backend on iOS/iPadOS/tvOS",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile index.js -o osanalytics.js -S -c"
  },
  "devDependencies": {
    "@types/frida-gum": "^19.0.0",
    "frida-compile": "^16.4.2"
  },
  "dependencies": {
    "frida-objc-bridge": "^8.0.4"
  }
}
