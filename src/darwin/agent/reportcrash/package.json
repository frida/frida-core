{
  "name": "frida-reportcrash-agent",
  "version": "1.0.0",
  "description": "Agent used by Frida's Darwin backend on iOS/iPadOS/tvOS",
  "type": "module",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile index.js -o reportcrash.js -S -c"
  },
  "devDependencies": {
    "@types/frida-gum": "^19.0.0",
    "frida-compile": "^18.0.0"
  },
  "dependencies": {
    "frida-objc-bridge": "^8.0.4"
  }
}
